import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gravitationalpomodoro',
  appName: 'gravitational-pomodoro',
  webDir: 'dist'
};

export default config;
